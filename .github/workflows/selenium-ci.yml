name: Selenium CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch: {}
  schedule:
    - cron: '50 5 * * *' # Runs daily at 10:50 AM PKT

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Install Chrome (stable)
        uses: browser-actions/setup-chrome@v1

      - name: Show Chrome version
        run: chrome --version

      - name: Build project
        run: mvn -B -DskipTests package

      - name: Run ALL Selenium tests automatically
        run: |
          echo "🔍 Searching for all Selenium tests..."
          set -e
          classes=$(find src/test -name "*.java" | sed 's#src/test/##; s#/#.#g; s#\.java$##')
          for cls in $classes; do
            echo "▶ Running $cls..."
            xvfb-run -a mvn -q exec:java -Dexec.mainClass="$cls"
          done
